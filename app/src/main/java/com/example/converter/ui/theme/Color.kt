package com.example.converter.ui.theme

import androidx.compose.ui.graphics.Color

val White = Color(0xFFFFFFFF)
val TextMuted = Color(0xFF5A7060)        // secondary labels: "Live exchange rates", "From", "Rate", etc.
val TextValue = Color(0xFF7A9A80)        // "0.00" input values, exchange rate figures
val CardBackground = Color(0xFF141F15)   // Converter Card / Network Fees card
val InputBackground = Color(0xFF0F1910)  // From/To input fields
val AccentLime = Color(0xFFACF532)       // USD/EUR pills, swap icon, chevron accent
val DarkText = Color(0xFF0C140D)         // text on top of AccentLime, bottom bar background

// Background gradient (radial, on the root frame)
val GradientStart = Color(0xFF18281A)
val GradientMid = Color(0xFF0E180F)
val GradientEnd = Color(0xFF08080C)
